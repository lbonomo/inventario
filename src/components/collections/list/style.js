import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  container: {
    marginTop: '3rem',
    marginBottom: '2rem',
    width: '100%',
  },

  dataGridContainer: {
    height: 400,
    width: '100%',
    margin: '1rem 0',
  },
  cardItem: {
    padding: '0.5rem',
  },

}))

export default useStyles
